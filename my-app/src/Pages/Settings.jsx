import React from "react";

const Settings = () => {
    return(
        <>
        <h1>Settings</h1>
        <option>
        <select>Darkmode</select>
        <select>LightMode</select>
        </option>
        <button>Save changes</button>
        </>
    )
}

export default Settings